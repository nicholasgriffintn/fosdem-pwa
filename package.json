{
  "name": "fosdem-pwa",
  "version": "0.2025.1",
  "private": true,
  "packageManager": "pnpm@10.30.2",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "dev": "pnpm --filter @fosdem-pwa/web dev",
    "build": "pnpm --filter @fosdem-pwa/web build",
    "test": "pnpm -r --if-present test",
    "lint": "pnpm -r --if-present lint",
    "typecheck": "pnpm -r --if-present typecheck",
    "format": "pnpm --filter @fosdem-pwa/web format"
  }
}